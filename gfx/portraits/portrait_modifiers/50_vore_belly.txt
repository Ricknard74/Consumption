add_vore_belly = {
   selection_behavior = max
   usage = game

   vore_belly = {
      dna_modifiers = {
         morph = {
            mode = add
            gene = gene_bs_belly
            template = belly
            # value = {
            #    value = 1
            # #   multiply = {
            # #      add = digestion_modifier
            # #      divide = 10
            # #   }
            # }
            value = {
               value = 1
               # multiply = {
               #    add = 10 #vore_days
               #    divide = 10
               #    round = yes
               #    multiply = 5
               #    max = 50
               # }
            }
         }
         accessory = {
            mode = add
            gene = clothes
            template = no_clothes
            value = 0 # For the randomness to work correctly
            #try copying and pasting 01_legwear_base.txt and replace the conditions for removing the top. Try removing it piece by piece and see if the file is what doesn't work or something else
         }
      }

      
      weight = {
         base = 0
         modifier = {
            add = 1000
            OR = {     
               portrait_has_trait_trigger = { TRAIT = oral_predator }
               portrait_has_trait_trigger = { TRAIT = digesting }
               portrait_has_trait_trigger = { TRAIT = anal_predator }
               portrait_has_trait_trigger = { TRAIT = unbirth_predator }
               has_character_flag = portrait_belly
            }
         }        
      }
   }
}